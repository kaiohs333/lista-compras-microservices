{
  "name": "lista-compras-microservices",
  "version": "1.0.0",
  "description": "Sistema de lista de compras usando arquitetura de microsserviços.",
  "main": "client-demo.js",
  "scripts": {
    "install:all": "npm install && (cd api-gateway && npm install) && (cd services/user-service && npm install) && (cd services/item-service && npm install) && (cd services/list-service && npm install)",
    "test": "echo \"Execute os testes dentro de cada serviço.\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kaiohs333/lista-compras-microservices.git"
  },
  "keywords": [
    "microservices",
    "nodejs",
    "express"
  ],
  "author": "Kaio H. Silveira",
  "license": "",
  "dependencies": {
    "axios": "^1.7.2"
  }
}